{
  "version": "0.0.1-alpha.1",
  "name": "@envuso/authentication",
  "author": "Sam Parton @iDevelopThings",
  "description": "Envuso is a backend framework focusing building apis using Fastify and MongoDB support.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b --clean && tsc -b",
    "prep": "npm run build && npm version prerelease --preid=alpha"
  },
  "dependencies": {
    "@types/jsonwebtoken": "^8.5.1",
    "jsonwebtoken": "^8.5.1"
  },
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "@babel/preset-typescript": "*",
    "@envuso/app": "^0.0.1-alpha.8",
    "@envuso/common": "^0.0.1-alpha.10",
    "@envuso/routing": "^0.0.1-alpha.23",
    "@types/jest": "*",
    "babel-jest": "*",
    "fastify": "^3.14.2",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.4"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist"
}
