{
  "version": "0.0.1-alpha.20",
  "name": "@envuso/routing",
  "author": "Sam Parton @iDevelopThings",
  "description": "Envuso is a backend framework focusing building apis using Fastify and MongoDB support.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b --clean && tsc -b",
    "prep": "npm run build && npm version prerelease --preid=alpha"
  },
  "dependencies": {
    "class-validator": "^0.13.1",
    "http-status-codes": "^2.1.4",
    "class-transformer": "^0.4.0"
  },
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "@babel/preset-typescript": "*",
    "@types/jest": "*",
    "babel-jest": "*",
    "fastify": "^3.14.2",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.4",
    "@envuso/app": "^0.0.1-alpha.6",
    "@envuso/common": "^0.0.1-alpha.9"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist"
}
