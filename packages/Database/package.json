{
  "version": "0.0.1-alpha.2",
  "name": "@envuso/database",
  "author": "Sam Parton @iDevelopThings",
  "description": "Envuso is a backend framework focusing building apis using Fastify and MongoDB support.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b --clean && tsc -b",
    "prep": "npm run build && npm version prerelease --preid=alpha"
  },
  "dependencies": {
    "class-transformer": "^0.4.0",
    "mongodb": "^3.6.5",
    "pluralize": "^8.0.0"
  },
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "@babel/preset-typescript": "*",
    "@envuso/app": "^0.0.1-alpha.8",
    "@envuso/common": "^0.0.1-alpha.12",
    "@types/jest": "*",
    "@types/mongodb": "^3.6.12",
    "@types/pluralize": "^0.0.29",
    "babel-jest": "*",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.4"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist"
}
