{
  "version": "0.1.0-alpha.0",
  "name": "@envuso/storage",
  "author": "Sam Parton @iDevelopThings",
  "description": "Envuso is a backend framework focusing building apis using Fastify and MongoDB support.",
  "license": "MIT",
  "scripts": {
    "build": "tsc -b --clean && tsc -b",
    "prep": "npm run build && npm version prerelease --preid=alpha"
  },
  "dependencies": {
    "@types/dotenv": "^8.2.0",
    "aws-sdk": "^2.884.0",
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "@babel/preset-typescript": "*",
    "@envuso/app": "^0.1.0-alpha.0",
    "@envuso/common": "^0.0.1-alpha.13",
    "@envuso/routing": "^0.1.0-alpha.0",
    "@types/jest": "*",
    "babel-jest": "*",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.4"
  },
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "types": "dist"
}
