export declare class Classes {
    /**
     * Check if a class has been instantiated
     *
     * @param c
     * @returns {boolean}
     */
    static isInstantiated(c: any): boolean;
    /**
     * If a class has been instantiated, get the underlying constructor
     * Otherwise, return the constructor
     *
     * @param c
     * @returns {boolean}
     */
    static getConstructor(c: any): boolean;
}
